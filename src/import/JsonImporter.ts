import {ShaderityGraphNodeJson} from '../types/CommonType';

export default class JsonImporter {
  public static importJsonToNodes(nodesJson: ShaderityGraphNodeJson[]) {
    console.log(nodesJson);
  }
}
